const visibleTodo={
    "show_all":"showAll",
    "non_complete":'nonComplete',
    "completed":"completed"
}

const ShowTodo=()=>{
    const getTodo=(x)=>{
        //TODO
    }
    return (
    <>
        <button>全部</button>
        <button>已完成</button>
        <button>未完成</button>
    </>
    )
}

export default ShowTodo